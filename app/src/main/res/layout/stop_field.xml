<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <RelativeLayout
        android:id="@+id/frame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/remove"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerInParent="true"
            android:src="@drawable/ic_close_white_24dp" />

        <AutoCompleteTextView
            android:id="@+id/stop"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/stop_field_height"
            android:layout_alignParentStart="true"
            android:layout_margin="@dimen/stop_field_margin"
            android:layout_toStartOf="@id/remove"
            android:background="@color/colorStopField"
            android:inputType="text"
            android:paddingEnd="@dimen/stop_field_padding"
            android:paddingStart="@dimen/stop_field_padding"
            android:textColor="@android:color/white"
            android:textColorHint="@color/colorHint"
            android:textSize="@dimen/stop_field_text" />

    </RelativeLayout>
</layout>